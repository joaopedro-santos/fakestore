import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.suzantur.products',
  appName: 'Suzantur Products',
  webDir: 'dist/suzantur-products/browser'
};

export default config;
